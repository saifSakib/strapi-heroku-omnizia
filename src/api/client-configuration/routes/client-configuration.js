'use strict';

/**
 * client-configuration router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::client-configuration.client-configuration');
